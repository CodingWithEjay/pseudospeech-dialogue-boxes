// Set the entirety of the text to be printed
stringPrinted = stringText;

// Indicate that the dialogue has finished
finished = true;

// Stop the speech sound
sound_stop(sound_speech);

// Set alarm[0] off
alarm[0] = 0;
